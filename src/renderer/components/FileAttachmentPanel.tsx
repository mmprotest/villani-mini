import React from 'react';
export default function FileAttachmentPanel({taskId,files,onChanged}:{taskId:string;files:any[];onChanged:()=>void}){ return <div><h3>Files</h3><button onClick={async()=>{await window.villani.attachFile(taskId,{unsupported:true}); onChanged();}}>Attach file</button><ul>{(files||[]).map((f:any)=><li key={f.id}>{f.extractionStatus} {f.summary} {f.errorMessage}</li>)}</ul><div>File picker unsupported in this build.</div></div>; }
