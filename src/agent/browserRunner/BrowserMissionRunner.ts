import { LocalOpenAIModelProvider } from '../../model/LocalOpenAIModelProvider';import { ManagedBrowser } from '../../browser/ManagedBrowser';import { BrowserToolExecutor } from '../../browser/tools/browserToolExecutor';import { BrowserToolLifecycle } from '../../browser/tools/browserToolLifecycle';import { browserMissionStore } from '../../browser/browserMissionStore';import type { BrowserMissionState, BrowserAgentMode } from './BrowserMissionState';import { buildBrowserContextPacket } from './BrowserContextBudget';import { decideBrowserStop } from './BrowserStopDecider';import { BrowserFailureClassifier } from './BrowserFailureClassifier';import { BrowserDebugRecorder } from './BrowserDebugRecorder';
export class BrowserMissionRunner{private sessions=new Map<string,BrowserMissionState>(); private listeners=new Set<(e:any)=>void>(); private failures=new BrowserFailureClassifier(); private debug=new BrowserDebugRecorder(); constructor(private provider=new LocalOpenAIModelProvider(), private browser=new ManagedBrowser()){}
onEvent(cb:(e:any)=>void){this.listeners.add(cb); return ()=>this.listeners.delete(cb);} private emit(s:BrowserMissionState,type:any,summary:string,payload:any={}){const ev={id:`be_${Date.now()}`,missionId:s.missionId,taskId:s.taskId,browserSessionId:s.browserSessionId,at:new Date().toISOString(),type,summary,payload}; browserMissionStore.appendEvent(s.missionId,ev); this.listeners.forEach((l)=>l(ev));}
start(input:{goal:string;mode:BrowserAgentMode;browserSessionId:string}){const missionId=`m_${Date.now()}`;const now=new Date().toISOString();const state:BrowserMissionState={missionId,taskId:missionId,browserSessionId:input.browserSessionId,userGoal:input.goal,mode:input.mode,status:'running',phase:'initializing',turnsUsed:0,toolCallsUsed:0,startedAt:now,updatedAt:now,recentObservations:[],pagesVisited:[],sourcesCollected:[],extractedNotes:[],failures:[]}; this.sessions.set(missionId,state); this.emit(state,'mission_started','Mission started'); void this.loop(missionId); return state;}
getState(id:string){return this.sessions.get(id)||browserMissionStore.loadState(id);} getEvents(_id:string){return [];} getTranscript(_id:string){return [];} pause(id:string){const s=this.must(id); s.status='paused'; this.save(s); return s;} resume(id:string){const s=this.must(id); s.status='running'; this.save(s); void this.loop(id); return s;} stop(id:string){const s=this.must(id); s.status='blocked'; s.stopReason='user_cancelled'; this.save(s); return s;} approve(id:string){return this.resume(id);} reject(id:string){return this.resume(id);} private must(id:string){const s=this.sessions.get(id); if(!s) throw new Error('mission_not_found'); return s;}
private save(s:BrowserMissionState){s.updatedAt=new Date().toISOString(); browserMissionStore.saveState(s); this.emit(s,'mission_state_saved','Mission state saved');}
private async loop(id:string){const s=this.must(id); const started=Date.now(); const exec=new BrowserToolExecutor(this.browser); const lifecycle=new BrowserToolLifecycle(exec,(e)=>this.emit(s,e.type,e.summary,e.payload),(k,v)=>this.debug.record(s.missionId,k,v)); while(s.status==='running'){s.turnsUsed++; const stop=decideBrowserStop(s,started); if(stop){s.stopReason=stop as any; s.status=stop==='paused_by_user'?'paused':'blocked'; this.emit(s,'stop_decision',`Stopped: ${stop}`); break;} const ctx=buildBrowserContextPacket(s); this.debug.record(s.missionId,'model_request',ctx); const res=await this.provider.createMessage({systemPrompt:'Use browser tools.',messages:[{role:'user',content:[{type:'text',text:JSON.stringify(ctx)}]} as any],tools:[] as any}); this.debug.record(s.missionId,'model_response',res); const text=res.message.content.find((b:any)=>b.type==='text')?.text||''; const maybe=text.match(/"tool"\s*:\s*"([^"]+)"/); if(maybe){const toolName=maybe[1]; const args=JSON.parse((text.match(/"input"\s*:\s*(\{[\s\S]*\})/)||[])[1]||'{}'); const out=await lifecycle.run(s,toolName,args); s.toolCallsUsed++; if(out.observation){s.lastObservation=out.observation;s.recentObservations.push(out.observation);} if(out.isError){const sig=`${toolName}:${out.content.slice(0,40)}`; const c=this.failures.add(sig); s.failures=[...this.failures.top().map((x)=>({signature:x.signature,count:x.count,lastAt:new Date().toISOString()}))]; if(c>=3){s.stopReason='repeated_tool_failure';s.status='blocked';break;}} } else {s.status='completed'; s.stopReason='completed'; browserMissionStore.saveFinal(s.missionId,{summary:text}); break;} this.save(s);} this.save(s);}}
